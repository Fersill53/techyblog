<!-- views/dashboard.handlebars -->

<h1>Welcome, {{username}}</h1>

<ul>
  {{#each posts}}
    <li>
      <h2>{{this.title}}</h2>
      <p>{{this.content}}</p>
    </li>
  {{/each}}
</ul>

<a href="/logout">Logout</a>